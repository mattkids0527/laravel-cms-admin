# Handoff：M005 前端工具鏈遷移（Vite + Tailwind Build）

- **日期**：2026-05-10
- **里程碑**：M005（completed）
- **關聯 bug**：BUG-001（同步根本解決）

---

## 完成內容

### T1：安裝 npm 套件
- 新增 `tailwindcss@^3`、`postcss`、`autoprefixer` 至 devDependencies
- 注意：npm 預設安裝 tailwindcss v4，需明確指定 `@^3`，因現有 blade 全部使用 v3 語法

### T2：建立 config 檔案
- 新建 `tailwind.config.js`：`darkMode: 'class'`，content 掃描 `resources/views/**/*.blade.php`、`app/Livewire/**/*.php`、`app/View/Components/**/*.php`
- 新建 `postcss.config.js`：tailwindcss + autoprefixer

### T3：更新 resources/css/app.css
- 加入 `@tailwind base`、`@tailwind components`、`@tailwind utilities`

### T4：更新 admin.blade.php
- 移除 `<script src="https://cdn.tailwindcss.com"></script>`
- 移除 `<script>tailwind.config = { darkMode: 'class' }</script>`
- 加入 `@vite(['resources/css/app.css', 'resources/js/app.js'])`

### T5：更新 guest.blade.php
- 移除 `<script src="https://cdn.tailwindcss.com"></script>`
- 加入 `@vite(['resources/css/app.css', 'resources/js/app.js'])`

### T6：Build 驗證
- `npm run build` 成功，產出：
  - `public/build/assets/app-DQ107VE1.css`（25.27 kB / gzip 4.83 kB）
  - `public/build/assets/app-CcNNqum8.js`（42.06 kB / gzip 16.58 kB）

### T7：恢復 Login.php navigate: true
- `app/Livewire/Admin/Auth/Login.php` line 45 改回 `navigate: true`
- BUG-001 根本解決（Tailwind CDN 移除，SPA 換頁不再有空窗期問題）

### T8：測試驗證
- 登入 SPA 換頁至 dashboard：版面正常
- sidebar 換頁：不跑版
- dark mode 切換：正常
- sidebar 收合/展開：正常

---

## 目前狀態

- M001–M005 全部 completed
- Tailwind CDN 完全移除，改用 Vite build
- BUG-001 根本解決，`navigate: true` 已恢復
- 前端每次修改後需執行 `npm run build`（或 `npm run dev` 開啟 watch 模式）

---

## 下一步建議

- 尚無作用中 milestone，可依需求規劃新功能
- 待規劃功能（memory 記錄）：檔案上傳管理、CMS 內容管理、儀表板統計連資料庫、密碼重設、403 頁面
- 開發時建議使用 `npm run dev` 讓 Vite 自動偵測 CSS 變更，不需每次手動 build

---

## 風險與注意事項

- `tailwind.config.js` 的 `content` 路徑若有遺漏，部分 class 會被 purge 掉（build 後樣式消失）
- 目前 `admin.blade.php` 保留 native layout CSS（`.layout-root` 等），可視需求回歸 Tailwind class
- 正式部署前需執行 `npm run build`，確保 `public/build/` 包含最新產出
